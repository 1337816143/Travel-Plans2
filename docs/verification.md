# Foundation verification

The CI workflow must complete these checks before the foundation is treated as stable:

1. install the pnpm workspace;
2. type-check all packages;
3. run domain tests;
4. run planner-engine tests.
